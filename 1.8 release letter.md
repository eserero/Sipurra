# Komelia 1.8 Release Letter

## User Facing Changes

### Features
*   **Thumbnail Customization:** New sliders in Appearance settings to adjust thumbnail scaling (width and height) and row spacing.
*   **EPUB Reader Enhancements:**
    *   **Full-Text Search:** Integrated search capability with context snippets and absolute location markers.
    *   **Unified Navigation:** Combined Table of Contents and Bookmarks into a single, swipeable multi-tab dialog.
    *   **Local Bookmarks:** Added support for adding and managing bookmarks with local persistence.
    *   **Improved Loading:** New step-by-step loading screen that prioritizes local files for faster opening of downloaded books.
    *   **Swipable Settings:** Implemented animated, swipable tabs in reader settings and bottom sheets.
*   **Offline Improvements:** Local-first page loading and a new "server unavailable" dialog for better handling of network issues. - if the comic file is downloaded, it will be used even in online mode for faster loading.
*   **Visual Updates:** Refreshed project documentation with 33 new screenshots.
*   **New Application Icon:** Updated application icons and thumbnails for Android and Desktop.

### Bug Fixes
*   Fixed the "Delete downloaded" menu option not appearing when the app is online.
*   Fixed software keyboard behavior and theme consistency in EPUB search.
*   Resolved EPUB reader stability issues, including rare crashes (NPE) and audio synchronization bugs.
*   Fixed audio player theming and bookmark display consistency in non-immersive mode.

---

This release letter summarizes the changes in the upcoming 1.8 version, based on commits since 1.7 (fea73cc7).

# feat(ui): add thumbnail presentation settings (width/height gap and row spacing)
- Add cardWidthScale, cardHeightScale, and cardSpacingBelow to AppSettings and database.
- Implement three new sliders in Appearance Settings to control these values.
- Update Library cards and immersive screens to apply scaling and spacing.
- Synchronize immersive morphing cover animation with the new scaled sizes.
- Set default thumbnail size to 95% while allowing full 100% cell fill.

---

# fix(oneshot): show "Delete downloaded" when book is downloaded, not only when offline
The menu item was gated on `isOffline` instead of `book.downloaded`, causing
it to never appear when the app is online even if a local copy exists.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>

---

# feat(reader): epub loading screen with local-first file loading
Show a step-by-step loading screen while the epub reader initialises.
Fixes a bug where books downloaded to SAF folders were always re-fetched
from the server: SAF files have no direct filesystem path so
getBookLocalFilePath() returned null and fell through to the server
download branch.

- Add EpubLoadingScreen composable (all platforms) and EpubLoadingStep
  model to display in-progress / completed steps during epub init
- Wire loadingSteps into EpubScreen, KomgaEpubReaderState, TtsuReaderState
- Add hasLocalFile() to KomgaBookApi (default false); implemented in
  RemoteBookApi (checks offline repo) and OfflineBookApi (always true)
- Fix RemoteBookApi.downloadBookRawFile to prefer the locally downloaded
  file (including SAF URIs) over a network fetch when one exists
- Make PlatformFile.readChunked public so the core module can stream
  SAF-backed files without a filesystem path
- Epub3ReaderState.prepareEpubDirectory now labels the step correctly
  ("Loading from local file" vs "Downloading from server")

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>

---

# ui: implement swipable tabs in reader settings dialogs
- Replace static tab switching with HorizontalPager in Epub3SettingsCard and BottomSheetSettingsOverlay.
- Synchronize SecondaryTabRow with pager state for animated transitions.
- Ensure independent scrolling and focus clearing behavior is preserved within each pager page.
- Clean up unused imports and duplicated collectAsState.

---

# feat(offline): expose local file path retrieval to Book API
- Implement getBookLocalFilePath in RemoteBookApi.\n- Expose PlatformFile.localFilePath() as public across all platforms (Android, JVM, Wasm).\n- Enables direct access to downloaded book files for the reader.

---

# docs(readme): refresh project documentation with New UI 2 screenshots
- Rename "Fork Overview" to "What's New in this Fork" and upgrade headers to level 3.\n- Add 33 new screenshots covering Library, Home, Immersive Details, Comic Reader, Epub Reader, and Settings.\n- Remove obsolete Immersive screenshots.\n- Organize content into descriptive tables for improved horizontal layout.

---

# ui: hide keyboard on epub search and update image reader settings theme
- Hide software keyboard when starting a search in Epub3ContentDialog.\n- Match image reader settings background with epub reader (Color(43, 43, 43)).\n- Fix black tab background in image reader settings by using transparent container.

---

# feat(reader): local-first page loading and server-unavailable dialog
When a book is downloaded locally, serve page images from disk instead
of the network. On metadata fetch failure due to a network error, show a
dialog offering Retry / Go Offline / Cancel instead of a raw timeout error.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>

---

# fix(epub3): fix audio player theming and bookmark display in non-immersive mode
Audio player (mini + fullscreen):
- Non-immersive mode now uses haze/solid-surface background matching ReaderControlsCard
  instead of a tinted Surface with tonalElevation
- In fullscreen (controls hidden) hazeSource is deactivated and LocalHazeState is set to
  null so the mini player renders a clean solid surface color with no unsourced blur
- Extract NonImmersiveAudioBackground composable to commonMain for shared use

Content dialog / bookmarks:
- BookmarkRow shows chapter title + location index instead of a raw position number
- Dividers use a softer outlineVariant color across TOC, Bookmarks and Search tabs
- TOC row text color uses onSurface consistently; expand icon tinted with onSurfaceVariant
- Locator parsing in BookmarkRow is now wrapped in remember to avoid redundant work

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>

---

# feat(epub): improve search results UI
- Replace progression percentage with absolute location (Loc X of Y) in search results.
- Truncate search result context text to ensure highlighted term is visible within 2 lines.
- Normalize whitespace in search result snippets to improve readability.

---

# feat(epub3): add search capability to epub3 reader

---

# feat(epub3): unify TOC and Bookmarks into a bottom-sheet Content Dialog
- Combined Epub3TocDialog and Epub3BookmarksDialog into a single, swipeable Epub3ContentDialog with tabs.
- Updated Epub3ControlsCard to use a circular toggle button for bookmarks on the current page.
- Changed bookmark behavior: toggle between adding and removing for the current locator.
- Updated dialog and settings backgrounds to match the library screen's dark mode style (Color(43, 43, 43)).
- Added a vertical divider and improved spacing in the control card's action row.
- Improved bookmark matching logic in Epub3ReaderState to handle href and progression/position correctly.

---

# feat(epub3): add bookmark support with local persistence
- Implemented SQLite-based bookmark storage with a new 'epub_bookmarks' table.
- Added EpubBookmarkRepository and its Exposed implementation.
- Introduced Epub3BookmarksDialog for listing, adding, and deleting bookmarks.
- Integrated bookmark controls into the EPUB3 reader control panel (New UI 2).
- Updated Epub3ReaderState to handle bookmark CRUD operations and state.

---

# fix(epub3): prevent NPE crash and resync text on audio background advance
- Replace navigator!! with navigator?. in EpubView.go() to avoid
  NullPointerException when the navigator is transiently null
- Retry initializeNavigator() in finalizeProps() when navigator is null
  but the book/font settings haven't changed (covers cases where
  onDetachedFromWindow destroyed the navigator mid-session)
- In MediaOverlayController.attachView(), re-apply the current audio
  clip to the view immediately when attaching mid-playback, using
  getCurrentClip() for accuracy over the stale lastHighlightedClip

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>

---

# docs: add third-party components inventory feat: update application icons for Android and Desktop fix: ensure EpubView navigator is destroyed only when fragment manager state is valid

---
